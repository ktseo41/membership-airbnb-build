import React from 'react';
import ReactDOM from 'react-dom';
import { useState, useEffect } from 'react';
import styled from 'styled-components';

const Count = (props) => {
    return <section className="count"></section>;
};

export default Count;
